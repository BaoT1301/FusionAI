# Development — leave VITE_API_URL unset; Vite proxies /api → BACKEND_URL
# Production  — set VITE_API_URL to your deployed backend origin
VITE_API_URL=
VITE_WORKSPACE_ID=web-client

# Dev-server proxy target (only used by `vite dev`, not included in the build)
BACKEND_URL=http://localhost:5001
